# Dependencies Required:
Python
- scikit-learn
- streamlit
- pandas
- matplotlib
- numpy
# Usage
After installing the dependencies along with 'app.py' and 'main.py':
Run the development server in the command line
'streamlit run app.py'

